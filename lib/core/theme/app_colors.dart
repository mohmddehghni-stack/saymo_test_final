import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // =========================================================
  // PRIMARY BRAND COLORS
  // =========================================================

  static const Color primary = Color(0xFFE87984);
  static const Color primaryDark = Color(0xFFCF606C);

  // رنگ مکمل برند
  static const Color secondary = Color(0xFF8E7CFF);

  // =========================================================
  // BACKGROUNDS
  // =========================================================

  static const Color background = Color(0xFFF7F7FF);
  static const Color backgroundLight = Color(0xFFFFF5F5);
  static const Color surfacePrimary = Colors.white;
  static const Color surfaceSecondary = Color(0xFFFDFDFD);
  static const Color cardBackground = Colors.white;

  // =========================================================
  // TEXT COLORS
  // =========================================================

  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF5D4037);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textWhite = Colors.white;

  // =========================================================
  // STATUS COLORS
  // =========================================================

  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF42A5F5);

  // =========================================================
  // SHADOWS
  // =========================================================

  static const Color shadowLight = Color.fromARGB(18, 0, 0, 0);
  static const Color shadowMedium = Color.fromARGB(35, 0, 0, 0);

  // =========================================================
  // BORDERS & DIVIDERS
  // =========================================================

  static const Color border = Color(0xFFEAEAEA);
  static const Color divider = Color(0xFFF0F0F0);

  // =========================================================
  // PERIOD FEATURE
  // =========================================================

  static const Color periodPink = Color(0xFFE87984);
  static const Color periodOvulation = Color(0xFFF4A261);
  static const Color periodBackground = Color(0xFFFFF0F3);

  // =========================================================
  // VIDEO FEATURE
  // =========================================================

  static const Color videoBackground = Color(0xFF1A0E2A);
  static const Color videoSurface = Color(0xFF121212);
  static const Color videoAccent = Colors.pinkAccent;
  static const Color videoOverlay = Color.fromARGB(120, 0, 0, 0);

  // =========================================================
  // CHAT FEATURE
  // =========================================================

  static const Color myMessage = Color(0xFFE87984);
  static const Color partnerMessage = Color(0xFFF1F1F1);

  // =========================================================
  // MOOD COLORS
  // =========================================================

  static const Color happy = Color(0xFFFFD54F);
  static const Color sad = Color(0xFF90CAF9);
  static const Color angry = Color(0xFFEF5350);
  static const Color hungry = Color(0xFFFFB74D);
  static const Color bored = Color(0xFFB39DDB);
  static const Color energetic = Color(0xFF81C784);

  // =========================================================
  // BUTTONS
  // =========================================================

  static const Color buttonPrimary = Color(0xFFE87984);
  static const Color buttonDisabled = Color(0xFFCCCCCC);

  // =========================================================
  // ICONS
  // =========================================================

  static const Color iconPrimary = Color(0xFFCF606C);
  static const Color iconSecondary = Color(0xFF757575);
}
